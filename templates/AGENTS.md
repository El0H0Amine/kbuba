# {{PROJECT}} (auto-read entry point for GPT agents)

Read `CLAUDE.md` at the repo root and obey it in full: the same behavior
rules, role selection (`ROLE=CONDUCTOR` token -> Conductor, otherwise
Implementer awaiting a TASK packet), and the rules for every agent apply
to GPT conversations identically.
