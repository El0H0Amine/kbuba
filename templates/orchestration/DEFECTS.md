# DEFECTS - {{PROJECT}} defect evidence ledger

> One entry per defect: `## D-<nnn> - <title>` with status
> (OPEN/CLOSED(date)), symptom, root cause, fix commit, and the lesson if
> it binds future work. Open defects are never rotated out. Run the
> ledger guard before every commit touching this file (CONDUCTOR.md §9).

(no defects recorded yet)
